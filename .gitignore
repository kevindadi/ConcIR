**/target
Cargo.lock

.cursor
.agents

.DS_Store
# Insta pending snapshots are transient; committed goldens in
# tests/snapshots/*.snap are tracked.
*.snap.new
*.pending-snap

# Python bytecode / caches (scripts and anywhere else)
__pycache__/
*.py[cod]

**.exit
**.stderr
**.stdout

# Local experiment trees (pilots, raw artifacts, real-case runs). Keep on disk,
# do not track.
experiments/
